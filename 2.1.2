# Given an instance of a Foo object, show two ways to print 'I am a Foo 
# object.' without hardcoding the word Foo.

# Solution:

class Foo:

    def __init__(self, name):
        self.name = name
        type_name = self.__class__.__name__
        print(f'I am a {type_name} object.')
        print(f'I am a {type(self).__name__} object.')


# Example:

food = Foo('Food')